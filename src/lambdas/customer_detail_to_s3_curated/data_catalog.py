column_comments = {
    "customer_detail":{
        "dynamo_user_key": "the id of the user from dynamo",
        "is_student": "If the customer is a student or not",
        "date": "date",
    }
}

schemas = {
    "customer_detail":{
        "dynamo_user_key": "string",
        "is_student": "boolean",
        "date": "date",
    }
}